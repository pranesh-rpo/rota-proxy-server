// Package main provides the entry point for the Rota Proxy Server
package main

import (
	"log"
	"os"

	"github.com/alpkeskin/rota/core/internal/server"
)

func main() {
	if err := server.Run(); err != nil {
		log.Printf("Failed to start server: %v", err)
		os.Exit(1)
	}
}
